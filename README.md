# Video to GIF Converter / Конвертер Видео в GIF

Веб-приложение для конвертации видео в GIF-анимацию и изображение из первого кадра.
Web application for converting videos to GIF animations and images from the first frame.

## Особенности / Features

- 🎬 Поддержка VK Видео, YouTube и других платформ
- ⏱️ Выбор начального тайминга (по умолчанию 10 секунд)
- 🎨 Двуязычный интерфейс (Русский/English)
- 📦 Автоматическое скачивание видео
- 🔄 Конвертация в GIF длиной 3 секунды
- 🖼️ Генерация изображения из первого кадра
- 🗑️ Автоматическая очистка временных файлов (через 60 минут)

## Требования / Requirements

- Python 3.8+
- ffmpeg (должен быть установлен в системе)
- yt-dlp

## Установка / Installation

### 1. Установите ffmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install ffmpeg
```

**Windows:**
Скачайте с https://ffmpeg.org/download.html

### 2. Установите Python зависимости

```bash
pip install -r requirements.txt
```

## Запуск / Usage

```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5500

## Использование / How to Use

1. Вставьте ссылку на видео (поддерживается VK Видео, YouTube и др.)
2. Укажите тайминг, с которого начать создание GIF (по умолчанию 10 секунд)
3. Нажмите "Создать GIF"
4. Скачайте готовый GIF и/или изображение из первого кадра

## Технологии / Tech Stack

- **Backend:** Python, Flask
- **Video Processing:** ffmpeg
- **Video Download:** yt-dlp
- **Frontend:** HTML, CSS, JavaScript

## Структура проекта / Project Structure

```
VideoGif/
├── app.py              # Flask приложение
├── requirements.txt    # Python зависимости
├── templates/
│   └── index.html     # Веб-интерфейс
└── temp/              # Временные файлы (создается автоматически)
```

## Примечания / Notes

- GIF создаются с частотой 15 FPS и шириной 480px
- Изображение из первого кадра создается в формате JPG с высоким качеством
- Временные файлы автоматически удаляются после 60 минут
- Для работы с VK Видео используется yt-dlp

## Поддержка платформ / Supported Platforms

- VK Видео (приоритетная поддержка)
- YouTube
- И многие другие платформы, поддерживаемые yt-dlp

## Лицензия / License

MIT License
